# Sovereign AI Wiki 日志

>> 本日志从 LMT-twister 主仓库 `docs/wiki/log.md` 精简而来,只保留持续学习
>> (LM 系列 / OML / Replay) 相关条目。完整历史见主仓库。

| 日期 | 事件 | 说明 |
|:-----|:-----|:-----|
| 2026-09-06 | 初始化知识系统 | Bootstrap 创建的持续学习模型日志 |
| 2026-09-06 | LM1 整合 Replay + OML + 因果学习 | 在 `tests/run_lm1_production.py` 中完成三大机制整合：1) 新增 `ReplayBuffer` 抗遗忘机制，每轮按比例回放已见任务；2) 集成 `CausalProposer`（因果价值函数：生成元新颖性 + do干预 + backdoor + 反事实 + 真实反馈）与 `CausalRLAgent`（因果RL：do-counterfactual credit assignment）；3) 实现评估反馈闭环：每轮对提议任务做 `_eval_task_acc`，将真实泛化表现回传给因果提议器；4) CLI 新增 `--proposer {value,causal,causal_rl}`、`--replay/--no-replay`、`--replay-ratio`、`--causal-rl-lr`。已验证三种提议器均可运行。 |
| 2026-09-08 | LM1 GPU 训练支持 + 8M 档跑测 | 服务器(2×A100-40GB, NGC 容器)GPU 直通后, 完成 lm1 的 GPU 化改造并在 GPU1 跑 8M 档(9.02M 参数)。系列 commit: `ff070ad`(--scale 档位 0.9M~2B + --threads) → `d595b37`(--device cuda + SwiftTDHead .to()) → `88baf80`(swifttd f_b1/f_b2 硬编码 CPU 修复) → `812aa51`(--tag 隔离 checkpoint/报告) → `124ad82`(migrate_eval GPU 下 extend_head_n 新模块搬移) → `7f55370`(--outer-lr 可配)。**结果**: 降 lr=3e-4 版 10 轮即达 lm1 自动达标线 — unseen 0.1250 / c4 0.1302 / S5迁移 0.123 / loss 收敛 ~0.99 (vs 1e-3 版 R10 发散 loss 2.57)。c4 0.13 远超家族历史锁死带(0.075-0.096)。报告: `results/gpu_lm1/lm1_8m_lr3e4_REPORT.md`。详见 git log + docs/wiki/current-status.md。 |
| 2026-09-10 | **LM4 电磁波持续学习启动** | 新方向: 把 headless 持续学习框架扩展到 **NASA CDAWeb 真实空间物理电磁波数据**。数据: Van Allen Probes RBSP-A EMFISIS(`DENSITY_EMFISIS-L4` 6s + `MAGNETOMETER_1SEC-GEI_EMFISIS-L3`), 2015 全年 DENSITY(408万点)+ 全年 MAG(537万点), 不重叠窗口 **127,134 个**(stride=32)。**关键设计**: ① 排除 `fpe`/`fuh`/`wpe_over_wce`(与 density 有解析关系 = 标签泄漏), 只用 `Magnitude`/`rms`(波活动)/`lambda`/`delta`; ② 物理状态 = log10(density) 分 6 个分位数域; ③ 时间对齐必须近邻匹配(精确时间戳全失败)。**GPU 结果**: joint 上限 0.4295 / naive 0.1667(= 随机, 完全坍缩) / replay(按域均衡, ratio 1.0) 0.3613±0.0803(5 seed)。文档: `docs/lm4_wave.md`, 脚本: `tests/{dl_local,dl_mag,run_lm4_wave,run_lm4_overnight}.sh.py`。 |
| 2026-09-10 | **LM4 修正: train/test 泄漏** | 首版用 `stride=4/window=32` → 相邻窗口重叠 28/32 步(87.5%), 而 train/test 随机切分。实测**每个测试窗口到最近训练窗口的索引距离中位数 = 1**, 100% 的测试窗口都有重叠 ≥24/32 步的"兄弟"在训练集 → 经典时间序列泄漏。MLP 探针: 随机切分 0.4533 vs 按时间切分 0.3637。**更严重**: 回放缓冲每域 2000 窗口因重叠几乎覆盖该域大片时间点, 测试窗口正是其近邻副本 → 专门虚增 replay(0.36 逼近 joint 0.43 是假的)。**修正**: `build_windows` stride 默认 = window(不重叠), 样本 101.7万 → 12.7万。夜间队列 `tests/run_lm4_overnight.sh` 重跑(14 seed + 3 档 ratio 消融)。**教训**: 滑动窗口有重叠时, 随机切分就是泄漏; 要么不重叠, 要么按时间切分留 gap。 |
| 2026-09-11 | **LM4 修正后结果 (25 run)** | 夜间队列跑完(stride=32 不重叠)。**ratio 消融** (n=3): 0.3→0.2501 / 0.5→0.3512 / 2.0→0.3955。**主配置 ratio 1.0, 16 seed: 0.3857 ± 0.0291 (CV 7.5%)**, 16/16 > naive, CV 从泄漏版 30.5% 收紧到 7.5%。**naive 25/25 全部精确 0.1667**(随机基线, 完全坍缩)。**joint 上限修正**: 5000 步版 0.3931 欠训练, **20000 步版 0.4456** 才是真上限 → 保留率 ratio 0.3/0.5/1.0/2.0 = 56.1%/78.8%/86.6%/88.8%, naive 37.4%。**天花板位置**: joint 各域 D2/D3(0.161/0.150)只到随机水平 → `Magnitude/rms/lambda/delta` 分辨不了中段密度, 需换特征(加 HFR 波谱 或 位置→L-shell)。ratio 2.0 补 10 seed 队列运行中(`run_lm4_overnight2.sh`)。汇总脚本 `tests/aggregate_lm4.py`。 |
| 2026-09-11 | **LM4 特征升级 + 最终统计** | **① ratio 消融最终版** (n=3/3/16/13): 0.3→0.2501 / 0.5→0.3512 / 1.0→0.3857 / **2.0→0.4008 (CV 5.7%)**; 保留率(相对 joint 20k=0.4456) 56.1%/78.8%/86.6%/**89.9%**; naive 35/35 精确 0.1667。**显著性**: 2.0 vs 1.0 的 Δ=+0.0151, t=1.57 (df≈27), 95% CI [-0.005,+0.035] → **统计上不可分辨**; 可确认的是单调性 0.3≪0.5<{1.0≈2.0}(0.3→1.0 是质变 +0.136, 之后饱和)。**② WFR 波功率谱数据就绪**: 逐个探测 28 个 RBSP-A EMFISIS 数据集后确认 `HFR-SPECTRA`/`WNA-SURVEY` 均为稀疏/突发(多数时段 no data), **`WFR-SPECTRAL-MATRIX-DIAGONAL-MERGED` 全年连续**(6s 采样同 DENSITY L4); 已下载 2015 全年 12 月 = **5,226,553 条** (N,6,65) log10 PSD, NaN 0.00%, 与 DENSITY 最近邻时差中位数 1.73s(≤3s 占 100%), 2.87GB 已入服务器 `data/wave/`(共 36 个 npz)。**③ 纠正误标**: `lambda`/`delta` 是磁场矢量经纬角, 不是卫星位置; L-shell 需重下 MAG `coordinates`。脚本 `tests/dl_wfr.py`。 |
| 2026-09-11 | **LM4 特征升级救活中段域 (WFR 波谱)** | **① A 阶段对照**: 仅 MAG 4 维 joint **0.4354** → 加 WFR 波功率谱(30 维) joint **0.7213** (D2 0.667 / D3 0.677) —— 原先 D2/D3 只到随机水平, 是**特征信息不足**而非模型问题。**② 挖出 MAG `-100000` 哨兵污染**: 该填充值是非文档化哨兵, 而 `np.abs()` 会把它洗成合理的正值极值 → `delta` std 878.9→15.3, `lambda` 884.7→97.0。修法: 在任何变换**之前**按量程建有效掩码, 并且不用 `abs()`。**③ 机制分析** (`docs/lm4_wave_analysis.md`): `|B|` 管两端 (D0vsD5 AUC 0.9986), 波谱管中段; 残差化证明二者独立 (B@285.8Hz R²=0.152)。提交 `52583a4`→`376eecc`→`37bf3c4`→`a82aecc`。 |
| 2026-09-11 | **LM4 天花板归因: 探针 vs SSM 池化** | 加 `--pool {last,mean,max,cat}` + `--agg-path` + `--joint-only`, 做天花板探针 `tests/probe_ceiling.py`: 窗口统计 (mean/std/first/last + 5 分位) + MLP h2048×3 达 **0.8130**, 而 WaveSSM 同域数只有 **0.7213** → **SSM 的末时刻池化在丢信息**。队列系统 `tests/lm4_queue.py` (argv 指纹断点续跑 + 看门狗 + 每卡单条串行队列), 日志按 runs 文件名区分。提交 `55b0633`→`2c7c617`→`d3aacb0`→`92265ae`→`46f5851`。 |
| 2026-09-11 | **LM4 两天队列结算 (18 run)** | 18 run 全部 `rc=0`。三条裁决: **① `agg-path` 未拉高天花板** (pool=last 0.7213 / cat 0.6681 / cat+agg 0.6525)。**② 0.70 是任务粒度的函数**: 3 域 0.8510 / 6 域 0.7213 / 8 域 0.6270 / 12 域 0.3674; 打乱域序 replay −0.054 (密度递增课程有效); L-shell 小幅正增益。**③ `agg-path` 价值在稳定性**: 基线 CV 10.6% vs agg-path **CV 3.0%** (方差比 F=11.1), 均值差 p=0.181 不显著。文档 `docs/lm4_two_days_results.md`。提交 `61ff62a`。 |
| 2026-09-14 | **LM4 因果线闭环: 发现 + 干预 + 反事实** | **① 因果发现** (`tests/lm4_causal_waves.py`, PC 骨架 + 条件互信息): 10 变量, 20 条稳定边 / 18 条漂移边。**关键正向验证: 算法独立恢复出偶极场定律 `logB—logL`**; 稳定边含 `log_dens—ele_*` (hiss 波物理)、`logB—mag_*`; 漂移边含 `log_dens—absmaglat` (只在部分 L-shell 段出现, 物理上合理)。**② 干预与反事实** (`tests/causal_intervene.py`): 物理先验定向 → 每节点对父节点线性回归 → 线性 SCM; `do(log_dens ×0.1) → ele_lo +0.893`; `do(ele_hi ×2) → 全部 0` (方向检验通过: ele_hi 无子节点); `do(logL +1) → log_dens −4.807`。反事实按 Pearl 三步 (残差 abduction)。拟合出的 `logB = −2.481·logL` 接近偶极定律 L⁻³ ✓。局限随结论同报 (DAG 为物理先验手设 / 机制是观测回归 / 缺太阳风·地磁·MLT 混杂 / 线性假设)。提交 `3288347`。 |
| 2026-09-14 | **LM4 移植 lm1 架构 (PLN + OML 双循环 + Reptile)** | 新增 `hibs_lnn/pln_head.py`: `PLNHead` (含 per-feature 步长 step_beta, Meta-SGD/IDBD)、`inner_adapt` (克隆头在 support 上适应 K 步, `create_graph=True`)、`oml_step`、`SwiftTDAdapter`。`run_lm4_wave.py` 加 `--cl-method {naive,replay,oml,oml2}` + `--head {linear,pln,swifttd}` (与 cl_method **解耦**)。移植中抓到 3 个真 bug: oml2 漏混 replay / 漏 `clip_grad_norm_` (二阶梯度炸编码器) / 合并写成每步做 (lm3 是每 10 步, 每步做则头永远积累不了知识)。设计与统一 CL 框架: `docs/lm4_unified_cl_design.md`。提交 `cbebf7e`。 |
| 2026-09-14 | **LM5 多模态: 文本 + 电磁波 + 因果** | `tests/run_lm5_mm.py` (386 行): 共享主干 + 模态各自 stem/head; `en/zh/code` 走 Embedding (下一 token CE), `wave`/`causal` 走 Linear (分类), 输出**跨模态遗忘矩阵**。因果域从 `S4WorldSCM` 生成 (G→cyc; G→obs; cyc→obs; obs,cyc→acc), **观测版 + 干预版双标签**。语料: Tiny Shakespeare / 红楼梦 / hermes-agent 源码。修 lm5 CUDA OOM (整测试集喂 SSM → 复数张量 ≈5GB, 改分批 bs=512)。提交 `004c21c`→`16b8bd5`→`264a46b`。 |
| 2026-09-14 | **LM4 探索裁定: SSM 是瓶颈 (推翻旧推论)** | 天花板探针在 3 个粒度上对决: **3 域 MLP 0.9159 vs SSM 0.8510 (+0.065) / 6 域 0.8065 vs 0.7213 (+0.085) / 12 域 0.6638 vs 0.3674 (+0.296)** —— 差距随任务粒度**单调扩大**, 说明 SSM 的递归在丢高频结构, 且**任何读出头都救不回来**: `--agg-path` 与 `--stat-input` 都只补回 0.011 (0.8617 vs 0.9159)。写 `docs/lm4_ssm_bottleneck_verdict.md`, **撤回**此前「不是模型瓶颈」的推论。提交 `42458ea`。 |
| 2026-09-14 | **LM4 骨干替换 + BatchNorm 漂移确诊 (一个 bug 伪装成三个结论)** | 换 StatMLP (窗口统计 + MLP): 3/6/12 域 0.9151 / 0.8104 / 0.6579, **三个粒度全部追平可达上限**。**关键诊断链**: ① 出现「天花板 vs 保留率权衡」假象 (MLP replay 0.2500/保留率 30.9% vs SSM 0.5952/82.6%); ② 容量扫描 (h32→h512) **连 h32 都只有 0.27** → 否定容量假说; ③ 真因 = **BatchNorm running statistics 随域漂移** (持续学习公认杀手)。`--norm {batchnorm,fixed,none}` 对照: batchnorm 0.2617/32% / **fixed 0.7308/90.2%** / none 0.1674/21%。**→ 撤回「架构权衡」结论**。新骨干两指标全面胜 SSM (replay +0.136, 保留率 +7.6pt), 8 seed 确认 **0.7297±0.0038 (CV 0.5%)** vs SSM 0.6126±0.0180。提交 `35b2052`→`b02f423`→`0feb844`→`5aa1fa0`→`964ea9e`。 |
| 2026-09-14 | **LM4 学习效率曲线 (一等指标)** | 按用户指示把「学习效率/更新效率」做成核心抗遗忘指标 (不以数据压缩为解释)。加 `--trace-every`: 每 N 步评当前域, 记录达标 (天花板 50%/90%) 所需步数。实测每域 **100–200 步**达自身天花板 90% (预算 1000 步), **无灾难性遗忘**; step6 平均 0.7277 vs SSM 0.5952。**重要执行缺口 (用户指出)**: 该曲线只在 1 个遗留 run 里开过 —— 承载全部结论的 **105 个 result tag 中 `efficiency`/`curves` 均为空**, 后续实验必须默认开启。提交 `957f596`。 |
| 2026-09-14 | **LM4 干净配置下的 CL 方法矩阵 — 推翻三个旧结论** | 三处混淆 (backbone / head / norm) 全部解耦后重跑 (6 域, joint 0.7948): 线性头 rr=0.5/1/2/5 = 0.7301/0.7300/0.7368/0.7305 (**replay 强度几乎不影响**); **PLN 头 0.7287** / **SwiftTD 头 0.7365** (与线性头无差异); **OML2+PLN 0.7401 全场最优**; naive 0.1762。**→ 三个旧结论全部作废, 真凶唯一**: 「OML 不适合持续学习」✗、「PLN 头是元凶」✗、「天花板 vs 保留率是架构权衡」✗ —— 全是 BN 漂移这一个 bug 的假象 (它在所有配置里都生效, 因此同时污染了方法/头/架构三类对比)。域数扫描: 3/6/12 域 replay 0.8702/0.7368/0.5634, 保留率 95.7%/92.7%/89.6%。设计约束: `oml/oml2` 需要可适应头 (否则崩), 已在参数解析处加守卫。文档 `docs/lm4_clean_cl_matrix.md`。提交 `64feea5`。 |
| 2026-09-14 | **LM4 跨年泛化 (2015 → 2016) + 两个指标 bug** | 新增 `--test-data` 用另一年数据做外部测试。**修两个指标 bug**: ① 域边界必须在训练集上求、测试集沿用 (各自求分位数 → naive 跨年得 **1.000**); ② 外部指标要在**全流程训完后**读 (刚训完某域时读该域必然 ≈1.0)。修后: **域内 2015 0.7400 → 跨年 2016 0.591** (naive 0.183, ≈随机), 说明学到可迁移结构 (+0.408 over chance)。提交 `ba6d6be`。 |
| 2026-09-14 | **LM5 骨干对照: 骨干选择是模态依赖的** | 3 seed × 2000 步对照 (MLP 新 vs SSM 旧): **文本 SSM 赢** (en +0.125, code +0.125) / **电磁波 MLP 赢** (+0.065) / **因果域无差别** (四个指标差 ≤0.005, 逐 seed 撞同一堵墙 → 任务瓶颈非架构瓶颈)。总平均 SSM 0.3117 > MLP 0.2876 → **「把 lm5 切到新骨干」只在 wave 上成立, 整体反而变差**。**被推翻的自身判断**: 「文本任务改成池化预测末位 token」是错的 —— 丢掉的正是该任务需要的词序信息。排除的混淆: 三 seed 的 BPE md5 相同 → 高方差是真实模型随机性。文档 `docs/lm5_backbone_verdict.md`。提交 `4dc3325`。 |
| 2026-09-14 | **LM5 指标修复 + 混合骨干** | **① 退化基线陷阱 (本轮第 4 次同类事件)**: `y_do` 类分布 `{0:0.001,1:0.029,2:0.015,3:0.165,4:0.789}` → 多数类基线 0.789, 模型原始准确率 0.734 **低于常数预测器**; 但平衡准确率 (宏平均召回) 0.518 vs 随机 0.2 说明有真实信号。**→ 类不平衡任务必须同时报原始 + 平衡准确率**。根因是 SCM 干预饱和 (`do_intervene` 把该 perm 的全部生成元并入已学 → acc 恒 ~0.95), 修为「均匀覆盖干预强度」后多数类 0.789→0.508。**② 混合骨干** `MultiModalHybrid`: 文本走 SSM + LayerNorm, 波/因果走统计 MLP。3 seed: 总平均 0.3133 (微胜 SSM 0.3117), 但**没拿到「两边最优」** —— wave 0.4583 vs 纯 MLP 0.5256 (文本通路的梯度与 MLP 波通路互相干扰)。提交 `2851658`→`c12a434`。 |
| 2026-09-14 | **价值函数驱动训练调度 (lm4 + lm5)** | 用户: 「lm4, lm5 里面都要有价值函数」。**发现缺口**: 价值函数提议器 (`CausalProposer`/`CausalRLAgent`) 在 LMT-twister 家族已验证到生产级 (V35.19→V35.22→LM1), 但**从未接到 lm4/lm5** —— 那里的域序写死 D0→D5, 没有「选择学什么」这一层; 学习效率曲线也几乎没开。**新增 `hibs_lnn/value_proposer.py`**: `V(p) = λ_sim·sim(p) + λ_con·con(p) + λ_cov·cov(p) + λ_fb·fb(p)` (简约性 = 物理描述子距离的不可归约度; 自洽性 = 与已发现因果结构的吻合度; 覆盖增量 = 1/(1+freq); **真实反馈 = 1−acc**, 即 V35 的 feedback_value)。保留 V35.19 三个坑: 候选池必须 >> 提议数 / λ_cov 倒 U 型 / 覆盖项必须真有频率差异。**lm4 接线**: 候选池 = 物理描述子 `[log10 密度, log10 偶极 L, sin 磁纬]` 切 18 个细区间, 每轮按 V softmax 采样 k 个 (步数按 `n_fine/(n_domains·k)` 归一化, 保证每轮数据量与固定臂相同); 评估仍用原 6 粗域保证可比。**lm5 接线**: 候选 = (域, 第几轮复习), 池 = 域数 × 轮数。**首轮结果 (单 seed, 6 域 replay)**: fixed **0.7300** / random 0.6360 / value 0.6172 → **固定顺序仍最强**; 但 **naive 臂: fixed 0.1762 vs value 0.3700** —— 提议器调度让「不作任何防遗忘处理」的遗忘大幅减轻 (+0.20), 与 LMT-twister 「价值函数的价值在迁移质量与稳定性而非同分布即时收益」方向一致。**顺带修一个固定域序下永不触发的潜伏 bug**: `for rd, (rids,) in replay_buf.items()` 假设条目为 1 元组, 而 wave/causal 存 `(X,y)`; 且 `if rd == "wave": continue` 在解包**之后**才检查还漏了 "causal"。提议器把 causal 排到第一 → 立刻暴露。提交 `bcb46e8`→`ad8195f`。 |
| 2026-09-14 | **LM4 价值函数三臂对照 (fixed / random / value × 多 seed)** | 承接上一条: 价值函数调度在 6 域上做多 seed 对照 (15/18 run)。**结果**: replay — fixed **0.7336±0.0063 (CV 0.9%)** / value 0.6862±0.0647 (CV 9.4%) / random 0.6529±0.0469 (CV 7.2%); naive — fixed **0.1756±0.0040** / value 0.4282±0.0355 / random 0.4307±0.0362。**① 价值函数没有超过固定域序** (0.6862 < 0.7336), 但**超过了自身对照随机** (0.6862 > 0.6529) → 提议器有效, 只是「密度递增」这个物理课程对本任务更优。**② 提议器打散顺序 → naive 遗忘大幅减轻 (+0.25, CV 8% 可信)**, 但 value 与 random 在 naive 上几乎相同 (0.4282 vs 0.4307) → **这个好处来自「打散顺序」本身, 不是价值函数的增量**; 价值函数的增量只在 replay 臂 (+0.033)。**与 LMT-twister 结论方向一致**: 价值函数是**稳定器/探索器, 不是精度提升器** (V35.20「S4 内最优 ≠ 迁移最优」/ V35.22「方差缩 3 倍」)。脚本 `tests/compare_value_proposer.py`。提交 `f5298b4`。 |
| 2026-09-14 | **重构 benchmark: 固定域序不作数, 改用任务流分布 (leo 指出)** | **问题 (leo)**: 价值函数与持续学习是为**复杂环境和边缘设备**服务的, 用固定顺序 (`D0→…→D5`) 当 benchmark 不成立 —— 它预设了三件真实智能体不会有的知识: ① 任务集已知 ② 最优顺序已知 ③ 单趟不复习; 而且**排序课程恰好是 replay 最有利的顺序** (把域间干扰压到最低), 因此系统性偏袒要被打败的基线。**重新统计也修正了我此前的读法**: replay 上 value vs fixed Δ=−0.042 **t=−1.74 不显著** (「固定序更强」是过度解读); value vs random Δ=+0.053 t=+1.76 不显著; 但 naive 上 value vs fixed Δ=+0.257 **t=+19.4 显著**, 且 **value 的 naive 方差是 random 的 0.22 倍** (CV 7.4% vs 15.5%) → **价值函数 = 方差稳定器, 不是均值提升器**, 原样复现 LMT-twister V35.22「缩 3 倍」。**新 benchmark (lm4 + lm5 同步实现)**: `--stream {fixed,perm,revisit,nonstationary}` 替代固定序 (fixed 降级为启发式基线); `--proposer {bins,random,random-matched,value,value-nofb}` —— 其中 **`random-matched` 是关键对照**: 用 `--freq-profile` 载入 value 臂实测的提议频率分布驱动 random 臂, 否则 value 的 `cov(p)=1/(1+freq)` 会主动均匀化覆盖, 两者的复习频率 profile 本就不同, `value vs random` 说不清是「更聪明」还是「复习更均匀」; `value-nofb` 隔离「探索」与「价值」。新指标反映边缘设备: **any-time 平均准确率** (边学边可用) / **最差遗忘界** (安全功能不能崩) / 达标步数 (学习效率) / buffer 大小。提交 `f5298b4`→`2576e9a`→(本批)。 |
| 2026-09-14 | **Benchmark 结算: 价值函数未能优于频率对齐对照 (负面结论)** | lm4+lm5 新 benchmark 全臂结算 (8+7 臂 × 3 seed, `docs/bm_benchmark_results.md`)。**核心对照 `value` vs `random-matched` (唯一干净对照)**: lm4 replay Δ=−0.0045 (t=−0.32) / any-time Δ=+0.0125 (t=+0.47) —— **不显著且数值略低**; lm4 **最差遗忘界 Δ=+0.1509 (t=+2.22) 显著更差**; lm5 replay Δ=+0.0030 (t=+0.07) 不显著, any-time Δ=+0.0361 (t=+2.06) 边缘显著更好, 最差遗忘界不显著 → **lm4/lm5 方向互相矛盾, 只能判「测不出稳定效应」**。**连混淆对照也赢不了**: `value` vs `random` (均匀随机) lm4 replay Δ=−0.0197 → 表现不佳**不是**频率分布差异造成的假象。**`value-nofb` 与 `value` 逐位相同** (三指标 Δ=+0.0000 / t=+0.00, 且提议频率向量完全一致) → **`λ_fb` (真实反馈 = 1−acc) 是死代码, 对调度零影响, 必须修**; 由此此前把 value 臂结果读作「价值函数(含反馈)有效」的推论不成立, 观测到的任何效果只能来自 `sim/con/cov` 三项。**「方差稳定器」说法同样被推翻**: 干净配置下 lm4 value replay CV 2.0% 并未小于 random (1.7%), lm5 value CV 25.6% 反而是全场最大 (random-matched 5.3%)。目前能站住的只有**「在线调度 > 预先定死的任务流」** (`value` vs `perm`: replay t=+3.44 / any-time t=+4.31 显著), 但 `random`/`random-matched` 同样具备该优势 (lm4 上还更大) → **是「调度」的功劳, 不是「价值函数」的功劳**。**顺带修一个真 bug**: lm4 `random-matched` 首次 3/3 rc=1 —— driver 把**嵌套**结构的 `lm4_wave_results.json` 整文件拷作 profile, 而加载器按平铺取值, 取不到就退化成整个 dict → `np.asarray(dict)` 崩 (`TypeError: float() argument must be a string or a real number, not 'dict'`), 且日志伪装成 `[freq-matched] 载入频率分布: 3 项` (3 = dict 顶层键数, 非候选数); lm5 结果文件是扁平结构故同臂正常。修为 平铺 → `replay.proposer_freq` 回退 → `{idx:freq}` 稠密化, **未重启 driver**, 只补跑失败的 3 个 run, 全 rc=0 (`results/bm_stage2_rerun.log`)。**口径提醒**: `n=3` 下 ddof=0 会把 `value` vs `random` 的 t=−1.66 抬成 −2.04「显著」, 本报告统一用 ddof=1。脚本 `tests/analyze_bm_full.py`。 |
| 2026-09-14 | **发现价值函数实现是半成品: 4 项里 2 项恒为常数 (结果全部作废)** | ★ 这是本轮最重要的**自我纠错**。跑完 lm4/lm5 的 benchmark 后查出三处反常: ① `value` 与 `value-nofb` 的提议序列**逐位相同**; ② `value` 与 `random-matched` 几乎一样; ③ `value_cv` 仅 0.27 (区分度极低)。**根因两条**: **(a) 预生成 bug** — lm4 的整个 schedule 在训练开始前一次性生成, 导致 `observe()` 从未在校内被调用 → `acc` 全 NaN → `fb(p)≡1.0`, 且 `learned` 为空 → `sim(p)≡1.0`。**(b) `con(p)` 根本没实现** — `set_consistency()` 从未被调用, 自洽性项恒为 1.0。于是实跑的 `V ≈ 常数 + λ_cov/(1+freq)`, **退化成一个『均匀化采样器』, 与价值函数无关**。这同时踩中了 V35.19 记录的坑 1 (候选池必须 >> 提议数; 实测 fine-bins 18 配 18 次提议 = 1:1) 与 λ 失衡。**修复**: lm4 改为**在线**逐轮提案 (每轮训练完立刻 observe 再提案下一轮), 已验证 value 与 value-nofb 现在发散。**仍待修**: ① 实现 `con(p)` —— lm4 已有现成的自洽性材料: 因果发现给出的 **20 条稳定边** (含偶极定律 `logB—logL`), 候选区间的自洽性 = 其观测符合已发现稳定边的比例, 这正是 leo 理论「简约点=不可归约的原理; 自洽性=彼此不矛盾」在 lm4 上的直接落地; ② 锐化 `sim(p)` (低维描述子下 `exp(-d/σ)≈0` 使 sim≡1); ③ 放大池/提议比到 `--fine-bins 60+`。**在修完前, 『价值函数有没有用』没有答案。** |
| 2026-09-14 | **价值函数三处修复完成: 四项全部激活, 修正版 benchmark 已启动** | 接上条。**修复① `con(p)` 真正实现** — lm4 用因果发现的稳定关系 (密度↔电场波 / 偶极场 B∝L⁻³ / 频段内部) 算候选区间的自洽性; lm5 按模态给 (wave 用半数据 vs 全数据的相关强度比; causal 用 SCM 父结构; text 无结构可查→1.0, **明确标注未实现**)。**修复② `con` 的定义从二值改成连续强度** — 第一版写的是『关系是否成立』的二值判断, 实测**处处为真 → con ≡ 1.0 → 该项仍是死的** (std=0)。改为『区间内相关强度 / 全局相关强度』且**符号翻转直接清零**后, std 0.0 → **0.118**。**修复③ `sim(p)` 的 sigma 自适应** — 固定 sigma=0.5 在低维描述子下使 `exp(-d/σ)≈0` → sim≡1.0; 改为取最近邻距离中位数后 std=0.318。**结果**: 四项 term_std 全部 > 0 (sim 0.318 / con 0.137 / cov 0.250 / fb 0.478), `value_cv` 0.089 → **0.4711 (区分度 ×5.3)** —— **现在这才是真正的价值函数**。候选池也从 `fine-bins 18` 放大到 **60** (修 V35.19 坑1: 池必须 >> 提议数)。修正版 benchmark (`bm2_driver.sh` → `results/BM2_DONE`): lm4 value/value-nofb/random/random-matched × 3 seed, 以及 lm5 同套; 完成门控 cron (`bff2174e8bef`) 已更新为 DONE2 状态。**诊断字段 `proposer_stats.term_std` 已固化** —— 以后任何一项 std=0 就说明那一项是死的, 这是本轮踩坑的直接产物。 |
